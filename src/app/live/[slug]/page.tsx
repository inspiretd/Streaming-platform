import Link from 'next/link';
import { ArrowLeft, Heart, Radio } from 'lucide-react';
import { TomoshaPlayer } from '@/components/TomoshaPlayer';
import { demoChannels } from '@/lib/demo';
export default function ChannelPage({ params }: { params: { slug: string } }) { const channel = demoChannels.find((item) => item.id === params.slug) ?? demoChannels[0]; return <main className="app-shell"><header className="topbar"><Link href="/" className="brand"><span className="brand-glyph">T</span><span>TOMOSHA</span></Link><Link href="/live" className="text-link"><ArrowLeft size={16} /> Back to live</Link></header><section className="player-page"><TomoshaPlayer channelId={channel.id} title={channel.name} /><div className="player-copy"><div><p className="eyebrow"><Radio size={12} /> {channel.live ? 'Live signal' : 'Offline fixture'}</p><h1>{channel.name}</h1><p>{channel.program}. Playback is session-resolved and rights-gated.</p></div><button className="secondary-button"><Heart size={16} /> Add to favorites</button></div><div className="rights-note"><strong>Availability notice</strong><span>No real provider URL is bundled or returned in demo mode.</span></div></section></main>; }
