# Provider integration

Only rights-confirmed sources may be connected. Admin imports must remain server-side, accept HTTPS by default, reject private hosts and adult content, enforce size and entry limits, and redact credentials from logs. The browser receives playback metadata only after access and rights checks.
