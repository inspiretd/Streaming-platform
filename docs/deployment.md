# Deployment

Deploy the Next.js app to Vercel or Cloudflare with environment variables configured in the platform dashboard. Do not commit `.env.local`, playlists, cookies, tokens, or provider account data. Supabase migrations and seed fixtures are introduced in the persistence phase.
